{
  "name": "@golden-path-demo/api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers API for Golden Path Demo",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250115.0",
    "vitest": "^1.1.0",
    "wrangler": "^3.91.0"
  }
}
